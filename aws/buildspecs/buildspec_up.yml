version: 0.2

env:
  variables:
    CODE_SRC_DIR: '.'
    TF_VERSION: '1.6.6'

phases:
  install:
    runtime-versions:
      python: 3.12
      ruby: 3.2
    commands:
      - 'curl -s -qL -o terraform.zip https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip'
      - 'unzip -o terraform.zip'
      - 'mv terraform /bin'
      - 'rm terraform.zip'
      - 'cd ${CODEBUILD_SRC_DIR}/${CODE_SRC_DIR}/terraform'
      - 'bundle install ${CODEBUILD_SRC_DIR}/${CODE_SRC_DIR}/terraform/Gemfile'
  build:
    commands:
      - 'cd ${CODEBUILD_SRC_DIR}/${CODE_SRC_DIR}/terraform'
      - 'echo ## TERRAFORM APPLY : Starting with the Terraform Apply'
      - 'terraspace apply -input=false tfapply'

artifacts:
  files:
    - '**/*'

cache:
  paths:
    - '/bin'
    - '/usr/local/bin'
    - '/root/.terraform'
    - '/root/.cache'
    - '/root/.gem'
    - '/usr/local/bundle'
