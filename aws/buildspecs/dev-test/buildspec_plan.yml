version: 0.2

env:
  variables:
    CODE_SRC_DIR: '.'
    TF_VERSION: '1.6.6'
    PYTHON_VERSION: '3.12'
    RUBY_VERSION: '3.2'

phases:
  install:
    runtime-versions:
      python: $PYTHON_VERSION
      ruby: $RUBY_VERSION
    commands:
      # - 'cat ~/.aws/config'
      # - 'cat ~/.aws/credentials'
      - 'aws configure list'
      - 'aws s3 ls s3://terraform-state-891377212104-eu-central-1-test/'
      # - 'yum install -y yum-utils'
      # - 'yum-config-manager --add-repo https://rpm.releases.hashicorp.com/AmazonLinux/hashicorp.repo'
      # - 'yum -y install terraform'
      # - 'bundle install --gemfile ${CODEBUILD_SRC_DIR}/terraform/Gemfile'
  build:
    commands:
      # - 'cd ${CODEBUILD_SRC_DIR}/terraform'
      # - 'echo ## TERRASPACE PLAN : Generate the Terraform Plan'
      - 'TF_LOG=DEBUG terraspace all plan --out "tf.plan"'

artifacts:
  files:
    - '**/*'
